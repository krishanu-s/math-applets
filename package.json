{
  "name": "ts_proj",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/*_scene.ts src/rust*.ts src/*rust*.ts --bundle --platform=browser --outdir=dist --format=esm --define:import.meta.url='\"\"'",
    "build:dev": "esbuild src/rust_scene.ts --bundle --platform=browser --outdir=dist --format=esm --sourcemap",
    "build-rust": "cd rust-calc/ && wasm-pack build --target web --release && cd ../",
    "copy-wasm": "cp rust-calc/pkg/rust_calc_bg.wasm dist/. && cp rust-calc/pkg/rust_calc.js dist/rust-calc-bundle.js"
  },
  "type": "module",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "numpy-ts": "^0.12.0",
    "rust-calc": "file:./rust-calc/pkg"
  },
  "devDependencies": {
    "esbuild": "^0.27.2",
    "typescript": "^5.9.3"
  }
}
